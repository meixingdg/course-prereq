# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Course
  connector: csv
  connector_options:
    encoding: utf-8
    # TSV is specified using an extra parameter of the Python csv module.
    import_options:
      dialect: excel-tab
    export_options:
      dialect: excel-tab
    column_list: [semester, year, name, code, description, isCommIntense, minCredits, gradeType, maxCredits, prereqs, departmentName, departmentCode]
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: code
      external_name: code
      # Type: Integer Stats: 535 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: departmentCode
      external_name: departmentCode
      # Type: String Stats: 535 properties of this type in this kind.

    - property: departmentName
      external_name: departmentName
      # Type: String Stats: 535 properties of this type in this kind.

    - property: description
      external_name: description
      # Type: Text Stats: 535 properties of this type in this kind.
      import_transform: db.Text

    - property: gradeType
      external_name: gradeType
      # Type: String Stats: 535 properties of this type in this kind.

    - property: isCommIntense
      external_name: isCommIntense
      # Type: Boolean Stats: 535 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: maxCredits
      external_name: maxCredits
      # Type: Integer Stats: 535 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: minCredits
      external_name: minCredits
      # Type: Integer Stats: 535 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: name
      external_name: name
      # Type: String Stats: 535 properties of this type in this kind.

    - property: prereqs
      external_name: prereqs
      # Type: Text Stats: 535 properties of this type in this kind.
      import_transform: db.Text

    - property: semester
      external_name: semester
      # Type: String Stats: 535 properties of this type in this kind.

    - property: year
      external_name: year
      # Type: Integer Stats: 535 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

